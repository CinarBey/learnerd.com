/* Define the body and its background color */
body {
  background-color: #f5f5f5;
  font-family: Arial, sans-serif;
  font-size: 16px;
}

/* Define the header and its styling */
header {
  background-color: #333;
  color: #fff;
  padding: 20px;
}

header h1 {
  margin: 0;
}

/* Define the navigation bar and its styling */
nav {
  background-color: #444;
  color: #fff;
  padding: 10px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

nav a {
  color: #fff;
  text-decoration: none;
  padding: 5px;
}

/* Define the main content section and its styling */
main {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  margin-top: 20px;
}

section {
  width: 30%;
  margin-bottom: 20px;
  background-color: #fff;
  padding: 20px;
  border-radius: 5px;
  box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
}

section h2 {
  margin-top: 0;
}

/* Define the footer and its styling */
footer {
  background-color: #333;
  color: #fff;
  padding: 10px;
  text-align: center;
}
